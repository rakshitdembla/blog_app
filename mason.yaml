# Register bricks which can be consumed via the Mason CLI.
# Run "mason get" to install all registered bricks.

bricks:
  screen_helper: 0.1.0+8  # Using the exact version from BrickHub

  # Example of importing a brick from a remote git repository
  # widget:
  #   git:
  #     url: https://github.com/felangel/mason.git
  #     path: bricks/widget
